<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="colorControlActivated">@color/controlActivated</item>

        <item name="android:textColor">@color/textColorPrimaryDark</item>
        <item name="android:textColorPrimary">@color/textColorPrimaryDark</item>
        <item name="android:textColorHighlight">@color/textColorHighlight</item>
        <item name="android:windowBackground">@color/windowBackground</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@color/appBarText</item>
        <item name="android:colorForegroundInverse">@color/plum</item>
        <item name="android:textColorSecondary">@color/appBarText</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
    </style>

    <style name="ThemeOverlay.AppCompat.navTheme">
        <!-- Color of text and icon when UNSELECTED -->
        <item name="android:textColorPrimary">@color/navItem</item>
        <item name="android:textColorSecondary">@color/navItem</item>

        <!-- Color of text and icon when SELECTED -->
        <item name="colorPrimary">@color/navItemSelected</item>
        <!-- Background color when SELECTED -->
        <item name="colorControlHighlight">@color/navItemHighlight</item>
    </style>

</resources>
